---
import SchemaLD from '../components/SchemaLD.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import PreFooterCTA from '../components/PreFooterCTA.astro';
import WhatsAppWidget from '../components/WhatsAppWidget.astro';

interface Props {
  title: string;
  description: string;
  canonical: string;
  schema?: Record<string, unknown>;
  andar?: 'roxo' | 'gold' | 'orange' | 'gray' | 'red' | 'none';
  activePath?: string;
  lcpImage?: string;
  ogImage?: string;
  isYMYL?: boolean;
  noindex?: boolean;
}

const {
  title,
  description,
  canonical,
  schema,
  andar = 'none',
  activePath = '',
  lcpImage,
  ogImage = '/images/logos/logo-livel-og.jpg',
  isYMYL = false,
  noindex = false,
} = Astro.props;

const siteUrl = 'https://studiotreinointeligente.com.br';
const fullOg  = ogImage.startsWith('http') ? ogImage : `${siteUrl}${ogImage}`;
---

<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <meta name="color-scheme" content="light" />

  <!-- Preload LCP image (com type para o browser saber se aceita o formato) -->
  {lcpImage && (
    <link
      rel="preload"
      as="image"
      href={lcpImage}
      type={lcpImage.endsWith('.avif') ? 'image/avif' : lcpImage.endsWith('.webp') ? 'image/webp' : undefined}
      fetchpriority="high"
    />
  )}

  <!-- Preload fonte crítica para destravar o LCP (instrument-serif aparece no <h1>) -->
  <link rel="preload" href="/files/instrument-serif-latin-400-normal.woff2" as="font" type="font/woff2" crossorigin="anonymous" />
  <link rel="preload" href="/files/instrument-serif-latin-ext-400-normal.woff2" as="font" type="font/woff2" crossorigin="anonymous" />

  <!-- Favicons -->
  <link rel="icon" href="/favicon.ico" sizes="any" />
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
  <link rel="icon" type="image/png" sizes="64x64" href="/favicon-64.png" />
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
  <link rel="manifest" href="/site.webmanifest" />

  <!-- Referrer Policy via meta (funciona em todos os browsers) -->
  <meta name="referrer" content="strict-origin-when-cross-origin" />
  <!-- X-Frame-Options e X-Content-Type-Options são aplicados via HTTP headers (_headers / vercel.json) -->

  <!-- SEO -->
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="canonical" href={canonical} />
  {noindex && <meta name="robots" content="noindex, follow" />}
  {!noindex && isYMYL && <meta name="robots" content="index, follow, max-image-preview:large" />}

  <!-- Open Graph -->
  <meta property="og:type"        content="website" />
  <meta property="og:url"         content={canonical} />
  <meta property="og:title"       content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:image"       content={fullOg} />
  <meta property="og:locale"      content="pt_BR" />
  <meta property="og:site_name"   content="Studio Livel Treino Inteligente" />

  <!-- Twitter Card -->
  <meta name="twitter:card"        content="summary_large_image" />
  <meta name="twitter:title"       content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image"       content={fullOg} />

  <!-- Preconnect desnecessário: fontes são self-hosted -->
  <!-- DNS para WhatsApp (CTA) -->
  <link rel="dns-prefetch" href="//wa.me" />

  <!-- Schema JSON-LD -->
  {schema && <SchemaLD schema={schema} />}

  <!-- Fontes self-hosted: @font-face com URLs absolutas (evita resolução relativa à página) -->
  <style is:global>
    /* Latin (default — usado primeiro) */
    @font-face {
      font-family: 'Instrument Serif';
      font-style: normal;
      font-weight: 400;
      font-display: swap;
      unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      src: url('/files/instrument-serif-latin-400-normal.woff2') format('woff2'),
           url('/files/instrument-serif-latin-400-normal.woff') format('woff');
    }
    /* Latin-ext (subset com acentos PT) */
    @font-face {
      font-family: 'Instrument Serif';
      font-style: normal;
      font-weight: 400;
      font-display: swap;
      unicode-range: U+0100-024F, U+0259, U+1E00-1EFF, U+2020, U+20A0-20AB, U+20AD-20CF, U+2113, U+2C60-2C7F, U+A720-A7FF;
      src: url('/files/instrument-serif-latin-ext-400-normal.woff2') format('woff2'),
           url('/files/instrument-serif-latin-ext-400-normal.woff') format('woff');
    }
    @font-face {
      font-family: 'Instrument Serif';
      font-style: italic;
      font-weight: 400;
      font-display: swap;
      src: url('/files/instrument-serif-latin-400-italic.woff2') format('woff2'),
           url('/files/instrument-serif-latin-400-italic.woff') format('woff');
    }
  </style>

  <!-- CSS crítico inline + global -->
  <style is:global>
    @import '/src/styles/global.css';
  </style>
</head>

<body>
  <a href="#main-content" class="skip-link">Pular para o conteúdo principal</a>
  <Nav andar={andar} activePath={activePath} />
  <main id="main-content" tabindex="-1">
    <slot />
  </main>
  <PreFooterCTA />
  <Footer />
  <WhatsAppWidget />
</body>
</html>

<style is:global>
.skip-link {
  position: absolute; top: -100px; left: 20px;
  background: var(--roxo); color: #fff; padding: 8px 16px;
  border-radius: 0 0 var(--r-md) var(--r-md); font-weight: 600; font-size: 14px;
  text-decoration: none; z-index: 9999; transition: top 0.2s;
}
.skip-link:focus { top: 0; }
</style>
